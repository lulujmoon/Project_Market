<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mm.market.product.ProductMapper">
		<insert id="setInsert" parameterType="ProductVO">
			insert into product 
			(productNum, productName, username, productCategory, productDate, productHit, productHeart, productContent, 
			productPrice, productNego, locationCode)
			 values (#{productNum}, #{productName}, #{username}, #{productCategory}, NOW(), 0, 0, 
			 #{productContent}, #{productPrice}, 0, 0)
		</insert>
		
		<update id="setUpdate" parameterType="ProductVO">
			update product set productName=#{productName}, 
			productCategory=#{productCategory},
			productDate = #{productDate},
			productContent=#{productContent},
			productPrice=#{productPrice}
			where productNum=#{productNum}		
		</update>
</mapper>   

