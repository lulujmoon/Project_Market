<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mm.market.chat.ChatMapper">
		
	<select id="getChatList" parameterType="ChatVO" resultType="ChatVO">
		select * from Chat where productNum=#{productNum}
	</select>
	
	<insert id="saveChat" parameterType="ChatVO">
		insert into chat values (#{chatId}, #{productNum}, #{sender}, #{message}, #{sendDateTime})
	</insert>

	<insert id="saveRoom" parameterType="RoomVO">
		insert into chatRoom values (#{roomId}, #{productNum})
	</insert>

</mapper>   

